#!/usr/bin/env ruby
#
begin
  require_relative '../glance.rb'
  # Run the app!
  #
  puts "Hello, you're running glance from a gem!"
  Glance.run!

rescue LoadError => e
  #$:.unshift(path) if File.directory?(path) && !$:.include?(path)
  puts "Unable to load api"

end
